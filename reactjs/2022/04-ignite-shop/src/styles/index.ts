import { createStitches } from "@stitches/react";

export const { config, styled, globalCss, keyframes, getCssText, css, theme, createTheme } = createStitches({
  theme: {
    colors: {
      rocketseat: '#8257e6'
    }
  }
})